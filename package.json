{
  "name": "kiln-cms",
  "version": "0.2.0",
  "description": "Click-to-edit CMS for static sites. GitHub as the backend. Free hosting as the host.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test/",
    "deploy:worker": "cd worker && npx wrangler deploy"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "parse5": "^8.0.0",
    "dompurify": "^3.2.0",
    "wrangler": "^4.0.0"
  }
}
