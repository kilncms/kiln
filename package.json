{
  "name": "kiln",
  "version": "0.3.0",
  "description": "Click-to-edit CMS for static sites. GitHub as the backend. Free hosting as the host.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "kiln": "cli/index.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.js",
    "dev": "cd worker && npx wrangler dev --local --port 8787",
    "deploy:test": "npm run build && cd worker && npx wrangler deploy --env staging",
    "deploy:prod": "npm run build && npm test && cd worker && npx wrangler deploy && cd .. && node scripts/propagate-bundles.mjs",
    "deploy:worker": "cd worker && npx wrangler deploy",
    "propagate": "node scripts/propagate-bundles.mjs"
  },
  "dependencies": {
    "parse5": "^8.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "dompurify": "^3.2.0",
    "wrangler": "^4.0.0"
  }
}
